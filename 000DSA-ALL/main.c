#include "lvl-1-queue/Queuemain.h"
#include <stdio.h>

int main() {
  printf("Choose\n");
  int choice;
  scanf("%d", &choice);
  switch (choice) {
  case 1:

    queue_main_call();
  }
  return 0;
}
